package com.pdfvault.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography is fine for a utility app; declared here so it is
// easy to customise later in one place.
val AppTypography = Typography()
